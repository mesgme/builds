// This shows a simple build wrapper example, using the AnsiColor plugin.
node {
    stage ('echo') {
        echo "Hello jenkkins"
    }
    stage ('AWS sync up') {
        withCredentials( credentialsId: 'MESG AWS') {
            AWS("--region=eu-west-2 s3 ls")
        }
    }
}