// This shows a simple build wrapper example, using the AnsiColor plugin.
node {
    stage ('echo') {
        echo "Hello jenkkins"
    }
    stage ('AWS sync up') {
        sh 'aws s3 ls s3://mesg001'
    }
}